<div class="container">

        <div class="row">
            <div class="box">
                <div class="col-lg-12">
                    <hr>
                    <p class="text-center">
                        <%="#{t('.posts_tagged')} “#{h(@tag_name.titleize)}”".html_safe%>
                    </p>
                    <hr>
                </div>
                <% if @posts.any? %>
                    <%= render :partial => "/refinery/blog/shared/post", :collection => @posts %>
                    <%= will_paginate @posts %>
                <% else %>
                  <p><%= t('.no_blog_articles_yet') %></p>
                <% end %>
            </div>
        </div>
</div>
<% content_for :stylesheets, stylesheet_link_tag('refinery/blog/frontend') %>
