<div class="container">

        <div class="box">
            <div class="col-lg-12 text-center">
                <div id="carousel-example-generic" class="carousel slide">
                    <!-- Indicators -->
                     <%= raw @page.content_for(:carousel) %>
                </div>
                 <%= raw @page.content_for(:brand) %>
            </div>
        </div>
    
        <div class="box">
            <div class="col-lg-12">
                 <%= raw @page.content_for(:box1) %>
            </div>
        </div>
    
        <div class="box">
            <div class="col-lg-12">
            <%= raw @page.content_for(:box2) %>
            </div>
        </div>
    
</div>
<!-- /.container -->

<script>
$(document).on('ready', function () {
  $('.carousel').carousel({
      interval: 1000 //changes the speed
  })
});
</script>
