<% require 'devise/version' %>
<p><%= t('.greeting', :recipient => @resource.email, :default => "Dragi/draga #{@resource.name}") %></p>

<p><%= t('.instruction', :default => "Neko je zatražio promenu lozinke, to možeš da uradiš prateći link naveden ispod.") %></p>

<p><%= link_to t('.action', :default => "Promeni moju lozinku"), edit_password_url(@resource, :reset_password_token => (Devise::VERSION.start_with?('3.') ? @token : @resource.reset_password_token)) %></p>

<p><%= t('.instruction_2', :default => "Ako niste vi poslali zahtev, molimo vas ignorišite ovaj email.") %></p>
<p><%= t('.instruction_3', :default => "Vaša lozinka neće biti promenjena dok ne pristupite preko navedenog linka i ne kreirate novu lozinku.") %></p>
